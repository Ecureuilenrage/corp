{
    "name": "corp",
    "version": "0.1.0",
    "private": true,
    "description": "Bootstrap CLI local-first pour corp V1",
    "engines": {
        "node": ">=20.0.0"
    },
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "check:epic-closure": "npm run build && node dist/scripts/check-epic-closure.js",
        "test": "npm run build && node --test \"dist/tests/**/*.test.js\""
    },
    "bin": {
        "corp": "dist/apps/corp-cli/src/index.js"
    },
    "devDependencies": {
        "@types/node": "^24.5.2",
        "typescript": "^5.9.3"
    }
}
